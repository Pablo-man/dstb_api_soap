# SOAP Calculator Project
This project demonstrates how to consume a SOAP service to perform basic math operations.

## Configuration
- Java 8+
- `wsimport` command to generate classes from the WSDL

## How to use
1. Run the client with: `java com.example.calculator.SOAPCalculatorClient`